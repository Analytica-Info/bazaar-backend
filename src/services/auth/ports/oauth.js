'use strict';

/**
 * oauth.js — OAuthVerifier port definition (JSDoc only).
 *
 * Re-exports the typedef from _kernel/ports as a named anchor
 * so auth adapters can reference `require('../ports/oauth')` without
 * a deep relative path.
 *
 * @module auth/ports/oauth
 */

/**
 * @typedef {import('../../_kernel/ports').OAuthVerifier} OAuthVerifier
 */

module.exports = {};
